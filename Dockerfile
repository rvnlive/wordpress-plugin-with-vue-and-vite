# Use the official WordPress image as a parent image
FROM wordpress:latest

# Assuming you don't need additional steps for your WordPress setup,
# since the plugin will be mounted via docker-compose.yml
